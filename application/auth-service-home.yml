spring:
  flyway:
    enabled: true
    table: flyway_schema_history_auth
    locations: classpath:db/migration
    baseline-on-migrate: true
  datasource:
    host: localhost
    port: 5432   # ✅ Add this line
    username: postgres
    password: Test@123
    url: jdbc:postgresql://${spring.datasource.host}:${spring.datasource.port}/marketplugtest
    hikari:
      maximum-pool-size: 2
  redis:
    host: localhost
    port: 6379
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
  security:
    oauth2:
      resourceserver:
        jwt:
          secret-key: y3Lmzuo50yQ8vkKoe5Xg2TZGladh3P1F51SUCIYNs/7i4qvhHFZ1LuwsUraOnqA8

logging:
  level:
    org.flywaydb.core: DEBUG
